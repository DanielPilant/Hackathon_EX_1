# Backend_LLM (Friend A + Friend B)

# Core scraping + parsing
requests>=2.31.0
beautifulsoup4>=4.12.2
python-dotenv>=1.0.0

# Gemini SDK (used by: `import google.generativeai as genai`)
google-generativeai>=0.7.0

# Optional but recommended for JS-rendered sites
playwright>=1.43.0
